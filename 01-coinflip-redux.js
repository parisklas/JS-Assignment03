var coinFlip;
let times = prompt('How many times would you like to flip the coin?');

for (let i = 0; i <= times; i++) {
    var coinFlip = Math.round(Math.random());
    console.log(coinFlip);
}

if (coinFlip = 0) {
    console.log("Heads")
} else {
    console.log("Tails")
}
